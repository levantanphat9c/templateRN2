import { getStylesHook } from '@/Hooks';

export default getStylesHook({
  container: {
    borderRadius: 5,
    paddingVertical: 4,
    paddingHorizontal: 8,
    flexDirection: 'row',
    alignItems: 'center',
  },
  icon: {
    marginRight: 4,
  },
});
