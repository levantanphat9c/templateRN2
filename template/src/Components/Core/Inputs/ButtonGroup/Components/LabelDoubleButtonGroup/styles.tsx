import { getStylesHook } from '@/Hooks';

export default getStylesHook({
  button: {
    width: '100%',
    paddingVertical: 20,
    overflow: 'hidden',
  },

  leftButton: {
    marginRight: 16,
  },
});
